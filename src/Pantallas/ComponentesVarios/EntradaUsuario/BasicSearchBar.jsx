import { Form } from "react-bootstrap";

const SearchBar = () => {
    return (
        <Form.Group>
            <Form.Control></Form.Control>
        </Form.Group>
    );
};

export default SearchBar;